/* ============================================================
   BuiltSELL — Supabase configuration
   1. Create a project at https://supabase.com
   2. Settings → API → copy "Project URL" and the "anon public" key below
   3. Run the SQL in supabase-setup.sql (SQL Editor) to create the table + policies
   4. Authentication → Users → "Add user" to create your admin login (email + password)
   ============================================================ */

window.SUPABASE_URL = 'https://arcfguponxqreesezetz.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_PyYknuEXfbCCRYILTzx3Qg_M8M11dUF';

// Row id used to store the single site-content record (leave as 1)
window.SUPABASE_CONTENT_ID = 1;
